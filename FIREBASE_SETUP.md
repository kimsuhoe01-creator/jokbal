# 족발신선생 박닌점 실시간 주문 연결 설정 v2

고객 태블릿과 카운터 태블릿은 같은 GitHub Pages 프로젝트를 사용합니다.

- 고객 화면: `index.html`
- 카운터 화면: `counter.html`

별도 사이트를 배포할 필요는 없습니다.

## 1. Firebase 프로젝트 생성

1. Firebase Console에서 프로젝트를 만듭니다.
2. 웹 앱을 추가합니다.
3. 웹 앱 설정의 `firebaseConfig` 값을 복사합니다.
4. `js/firebase-config.js`의 `PASTE_...` 값을 실제 값으로 교체합니다.
5. Realtime Database를 생성한 뒤 표시되는 `databaseURL`을 정확히 입력합니다.

## 2. 익명 로그인 활성화

Firebase Console → Authentication → 로그인 방법에서 **익명(Anonymous)** 만 활성화합니다.

고객 태블릿과 카운터 태블릿 모두 로그인 화면 없이 익명 인증으로 연결됩니다.
이메일·비밀번호 직원 계정은 필요하지 않습니다.

GitHub Pages 도메인이 필요한 경우 Authentication → Settings → Authorized domains에 등록합니다.

## 3. Realtime Database 규칙 적용

Firebase Console → Realtime Database → 규칙에서 프로젝트의 `database.rules.json` 내용을 붙여넣고 게시합니다.

이 간편 버전은 로그인 화면 없이 운영하기 위해 익명 인증된 카운터 화면이 주문을 읽고 완료 처리할 수 있게 되어 있습니다. 카운터 주소는 매장 내부 태블릿에서만 사용하는 것을 권장합니다.

## 4. 배포

ZIP을 풀어 GitHub 저장소의 기존 파일을 전체 교체한 뒤 GitHub Pages를 다시 배포합니다.

- 고객 태블릿: 기존 사이트 주소
- 카운터 태블릿: 기존 사이트 주소 뒤에 `/counter.html`

예시:

```text
https://계정명.github.io/저장소명/
https://계정명.github.io/저장소명/counter.html
```

## 5. 고객 태블릿 사용

1. 처음 실행하면 테이블 선택창이 표시됩니다.
2. 다음 중 하나를 선택합니다.
   - `1-1` ~ `1-7`
   - `2-8` ~ `2-12`
   - `3-13`
3. 이후 변경할 때는 상단의 테이블 버튼을 누릅니다.
4. 메뉴를 담고 상단 우측의 **주문 완료** 버튼을 누릅니다.
5. 주문이 전송되면 장바구니가 자동으로 비워집니다.

## 6. 카운터 태블릿 사용

1. `/counter.html`을 엽니다.
2. 로그인 없이 바로 주문 대기 화면이 표시됩니다.
3. 처음 한 번 `알림 켜기`를 눌러 소리·알림 권한을 허용합니다.
4. 새 주문이 오면 화면 팝업, 소리, 진동, 브라우저 알림이 표시됩니다.
5. POS 입력이 끝나면 주문 카드의 `완료 처리`를 누릅니다.
6. 완료된 주문은 대기 목록에서 사라지고 완료 탭에 보관됩니다.

## 7. PWA 재설치

가로 고정이나 새 기능이 바로 반영되지 않으면 다음 순서로 처리합니다.

1. 기존 홈 화면 앱 삭제
2. Chrome에서 해당 사이트 데이터·캐시 삭제
3. 사이트 다시 접속
4. 홈 화면에 다시 설치

고객용과 카운터용은 같은 배포 안에 있지만 각각 별도 아이콘으로 설치할 수 있습니다.
